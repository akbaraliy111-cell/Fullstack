function longestCommonPrefix(arr){
    str = ''
    for (let word of arr){
        for (let i = 0; i < word.length; i++){
            if (word);
        }
    }
}

longestCommonPrefix(['flower','flow','flight']);