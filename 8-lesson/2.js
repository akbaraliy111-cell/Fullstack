function timeUntil(targetDay, targetHour) {
    const now = new Date();

    if (targetDay >= now.getDay()){
        
    }
    
    // 1. Maqsadli vaqtni yaratish (shu haftadagi)
    // 2. Agar o'tib ketgan bo'lsa — 7 kun qo'shish
    // 3. Farqni hisoblash
    // 4. Kun va soatga o'girish
}

timeUntil()